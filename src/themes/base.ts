import { Breakpoints } from "../interfaces"

const BREAK_POINTS: Breakpoints = {
    xs: 0,
    sm: 600,
    md: 900,
    lg: 1200,
    xl: 1536
};

const BASE_THEME = {
    breakpoints: BREAK_POINTS,
};

export {
    BASE_THEME
};